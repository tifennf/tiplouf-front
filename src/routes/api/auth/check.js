// import { auth } from '$lib/auth';

// const post = async (req) => {
// 	const { body } = req;

// 	const res = await auth(
// 		'http://localhost:5000/auth/check',
// 		JSON.stringify(body),
// 		'Failed to check session_id on auth service'
// 	);

// };

// export { post };
